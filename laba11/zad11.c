#include <stdio.h>
#include <stdlib.h>
#include "func11.h"
/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	printf(" %d \n", sum( 4, 5, 2, 3, 1));
	printf(" %d \n", max( 4, 5, 2, 3, 1));
	printf(" %d \n", min( 4, 5, 2, 3, 1));
	printf(" %lf", sra( 4, 5, 2, 3, 1));
	return 0;
}
